import { Calendartaskextended } from './calendartaskextended.model';

describe('Caltask', () => {
  it('should create an instance', () => {
    expect(new Calendartaskextended()).toBeTruthy();
  });
});
