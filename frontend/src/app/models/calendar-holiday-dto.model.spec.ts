import { CalendarHolidayDto } from './calendar-holiday-dto.model';

describe('CalendarHolidayDto', () => {
  it('should create an instance', () => {
    expect(new CalendarHolidayDto()).toBeTruthy();
  });
});
