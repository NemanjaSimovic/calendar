import { UserAvailabilityDto } from './user-availability-dto.model';

describe('UserAvailabilityDto', () => {
  it('should create an instance', () => {
    expect(new UserAvailabilityDto()).toBeTruthy();
  });
});
